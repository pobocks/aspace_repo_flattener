# aspace_repo_flattener
Merges ASpace DB with multiple repos into one with a single repo.

To use, copy config.yml.example to config.yml, fill in values for your ASpace's MySQL instance, and run the script.

Then, completely reindex your ASpace instance.
